version: ""
services:  
   shop:
    build: .
    environment:
      BOT_TOKEN: $BOT_TOKEN
      API_ID: $API_ID
      API_HASH: $API_HASH
      CHANNELS: $CHANNELS
      ADMINS: $ADMINS
      PICS: $PICS
      LOG_CHANNEL: $LOG_CHANNEL
      
      FORCE_SUB: $FORCE_SUB
      AUTH_CHANNEL: $AUTH_CHANNEL
      AUTH_USER: $AUTH_USER
